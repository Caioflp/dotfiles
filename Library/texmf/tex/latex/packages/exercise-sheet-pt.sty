\ProvidesPackage{exercise-sheet-pt}
\KOMAoptions{%
    fontsize=12pt,
    titlepage=false,
}

\usepackage{amsmath, mathtools, amssymb, amsfonts, amsthm, mathrsfs}
\usepackage[brazil]{babel}
\usepackage{microtype}
\usepackage{graphicx}


% Fonts
% \usepackage{newpxtext}
% \usepackage{mathpazo}
% \usepackage{pifont} % Fleurons
% \newcommand{\leafleft}{\ding{167}\enskip}
% \newcommand{\leafright}{\enskip\rotatebox[origin=c]{180}{\ding{167}}}


\usepackage{enumitem}
\setlist[description]{%
    leftmargin=0pt,
    topsep=.5em,               % space before start / after end of list
    itemsep=5pt,               % space between items
    font={\normalfont\textit}, % set the label font
}
\usepackage{global-macros}
\usepackage{hyperref}


% Format fonts for different text elements
% \setkomafont{title}{\Large}
% \setkomafont{subtitle}{\normalfont\itshape\large}
% \setkomafont{section}{\centering\normalfont\scshape\large\lsstyle}
\setkomafont{author}{\normalsize}
\setkomafont{date}{\normalsize}
\setkomafont{publishers}{\normalsize}
% \renewcommand{\sectionformat}{\leafleft\thesection.\enskip}
% Create new section command for embellishments
% \newcommand{\mysection}[1]{\section{#1\leafright}}


\newcommand{\sep}{ \bigskip \hrule \bigskip }

% Teorema-like environments
\newtheoremstyle{default} % name
        {0.7em}                    % Space above
        {0.5em}                    % Space below
        {\itshape}                   % Body font
        {}                           % Indent amount
        {\bfseries}                   % Teorema head font
        {}                          % Punctuation after theorem head
        {0.5em}                       % Space after theorem head
        {}  % Teorema head spec (can be left empty, meaning ‘normal’)
\theoremstyle{default} % default
\newtheorem{thm}{Teorema}[section]
\newtheorem{thmm}{Teorema}
\newtheorem*{thm*}{Teorema}
\newtheorem{lem}{Lema}[thm]
\newtheorem{lemm}{Lema}
\newtheorem*{lem*}{Lema}
\newtheorem{prop}{Proposição}[thm]
\newtheorem*{prop*}{Proposição}
\newtheorem{cor}{Corolário}[thm]
\newtheorem{corr}{Corolário}
\newtheorem*{axiom}{Axioma}

\newtheoremstyle{exercise} % name
        {0.7em}                    % Space above
        {0.5em}                    % Space below
        {\normalfont}
        {}                           % Indent amount
        {\bfseries}                   % Teorema head font
        {}                          % Punctuation after theorem head
        {0.5em}                       % Space after theorem head
        {}  % Teorema head spec (can be left empty, meaning ‘normal’)
\theoremstyle{exercise}
\newtheorem*{exercise*}{Exercício}
\newtheorem{exercise}{Exercício}
\newtheorem*{observation*}{Observação}
\newtheoremstyle{proof} % name
        {1em}                    % Space above
        {0.5em}                    % Space below
        {\normalfont}                   % Body font
        {}                           % Indent amount
        {\itshape}                   % Teorema head font
        {}                          % Punctuation after theorem head
        {0.5em}                       % Space after theorem head
        {}  % Teorema head spec (can be left empty, meaning ‘normal’)
\theoremstyle{proof}
\newtheorem*{sol}{Solução:}
\newtheoremstyle{fact} % name
        {0.7em}                    % Space above
        {0.5em}                    % Space below
        {\normalfont}                   % Body font
        {}                           % Indent amount
        {\scshape\lsstyle}                   % Teorema head font
        {}                          % Punctuation after theorem head
        {0.5em}                       % Space after theorem head
        {}  % Teorema head spec (can be left empty, meaning ‘normal’)
\theoremstyle{fact}
\newtheorem{fact}{Fato}
